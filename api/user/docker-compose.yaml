services:
  redis:
    image: redis:latest
    restart: on-failure
    container_name: user-api-redis
    env_file:
      - ${CONFIG_BASE_PATH}/api/accounts/secret/redis-secret.env
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 10s
      timeout: 5s
      start_period: 10s
      retries: 5
    networks:
      - api_user_network

  user-api:
  # todo remove registry id from compose
    image: cr.yandex/crpa0c9avqe31tmi0mfu/lockbox_user-api:latest
    container_name: user-api
    build:
      context: .
      args:
        DOCKER_BUILDKIT: 1
    restart: on-failure
    env_file:
      # component related env
      - ${CONFIG_BASE_PATH}/api/accounts/secret/postgres.env
      - ${CONFIG_BASE_PATH}/api/accounts/secret/redis-secret.env
      - ${CONFIG_BASE_PATH}/api/ssl.env
      # service related env
      - ${CONFIG_BASE_PATH}/service/postgres/postgres.env
      - ${CONFIG_BASE_PATH}/service/keycloak/realm.env
      - ${CONFIG_BASE_PATH}/service/keycloak/secret/client.env
      - ${CONFIG_BASE_PATH}/service/keycloak/secret/admin.env
    healthcheck:
      test: [ "CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/accounts/actuator/health" ]
      interval: 10s
      timeout: 5s
      start_period: 15s
      retries: 5
    environment:
      - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE}
    volumes:
      - ssl-certs:/etc/ssl/certs:ro
      - /etc/ssl/lockboxes:/etc/ssl/lockboxes
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - api_user_network
      - service_keycloak_network
      - service_postgres_network
      - service_elk

volumes:
  ssl-certs:
  postgres_data:

networks:
  service_elk:
    external: true
  api_user_network:
    external: true
  service_keycloak_network:
    external: true
  service_postgres_network:
    external: true
